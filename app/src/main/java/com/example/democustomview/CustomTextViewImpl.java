package com.example.democustomview;

import android.content.Context;
import android.util.AttributeSet;

import androidx.annotation.Nullable;

class CustomTextViewImpl extends CustomTextView {
    public CustomTextViewImpl(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }
}
